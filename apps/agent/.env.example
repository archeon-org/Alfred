# Standalone Agent Server contract. These values are placeholders only; use the
# Compose service names inside Docker or platform-provided private endpoints.
DATABASE_URI=postgres://alfred_agent:replace-with-a-url-safe-agent-database-password@postgres:5432/alfred_langgraph?sslmode=disable
REDIS_URI=redis://default:replace-with-a-url-safe-agent-redis-password@agent-redis:6379/0
LANGGRAPH_CLOUD_LICENSE_KEY=replace-with-a-valid-langgraph-license-key
