import React from "react";

export const GestureHandlerRootView = (props: any) =>
  React.createElement("GestureHandlerRootView", props, props.children);

export const ScrollView = (props: any) =>
  React.createElement("GestureHandlerScrollView", props, props.children);

export const Swipeable = (props: any) =>
  React.createElement("Swipeable", props, props.children);

export const RectButton = (props: any) =>
  React.createElement("RectButton", props, props.children);
